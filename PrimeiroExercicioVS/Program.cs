public class Program


{

    static void Main(string[] args)
    {
        /*var numerosMaisUm = new List<int>();
        var numeros = new List<int>();
        var strings = new List<string>();
        for (var contador = 0; contador < 10; contador++)
        {
            numeros.Add(contador);
            numerosMaisUm.Add(contador + 10);
            strings.Add(contador.ToString());
        }
        numeros.AddRange(numerosMaisUm);
        var tentativaUm = numeros.Remove(5 + 6);

        var indexString = strings.IndexOf(Console.ReadLine());
        var indexCinco = numeros.IndexOf(int.Parse(Console.ReadLine()));
        var posicaoDoMeuNome = numeros.ElementAt(3);
        var valorNaPosicaoTres = numeros.ElementAt(3);
        var valorPar = numeros.Find(numero => numero % 2 == 0);
        var valoresPares = numeros.Where(numero => numero % 2 == 0);
        var valoresParesDois = numeros.FindAll(numero => numero % 2 == 0); 21

        Console.WriteLine("Finalizou");


        Dev: var meuCachorro = new Cachorro("Vira-Lata", 1, 10);
        var outroCachorro = new Cachorro("Pinscher", 1, 3);
        outroCachorro.Peso++;
        var dataEHoraAtual = DateTime.Now;
        var dataEHoraAtualMaisUmDia = DateTime.Now.AddDays(1);
        var dataEHoraMenosUmMes = DateTime.Now.AddMonths(-1);
        Console.WriteLine("Insira sua data em mes/dia/ano");
        var dataDeNascimento = DateTime.Parse(Console.ReadLine()); 
        var datetimeEmString = dataDeNascimento.ToString("dd/MM/yyyy hh:mm.ss"); 
                                                                                
                                                                                
        var diferenca = dataEHoraAtual.Subtract(dataEHoraMenosUmMes);
        Console.WriteLine(diferenca);
        Console.WriteLine($"Você nasceu no dia: {dataDeNascimento.Day} do mês {dataDeNascimento.Month} e era: {dataDeNascimento.DayOfWeek}");
        Console.WriteLine("Finalizou");
        */
      
    }
}